{
  "name": "sweep_r2_samples",

  "_comment": "Sample density, at fixed lambda and fixed layout. Two things to read: whether the fit has converged in the sample count (it should flatten), and whether normalise_lambda is doing its job (the fairness energy should not drift with density, since a sweep over one axis must not silently move another).",

  "base": {
    "name": "r2_samples",
    "case": "../../data/thesis_doublevb.json",
    "fit": {
      "method": "least_squares",
      "layout_refinement": 3,
      "least_squares": { "lambda": 1e-2 }
    },
    "sampling": { "max_segment_length": 0.06, "max_sagitta": 0.006 },
    "refinement": { "max_triangle_area": 0.02, "min_angle_degrees": 25.0, "max_vertices": 3000 },
    "error": { "samples_per_face": 3, "nurbs_samples_per_side": 24 },
    "boundary": { "samples_per_edge": 16, "trim_samples_per_curve": 128 },
    "write_samples": false
  },

  "axes": {
    "fit.least_squares.samples_per_side": [16, 24, 32, 48, 64, 96, 128]
  }
}
